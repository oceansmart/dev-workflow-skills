# dev-workflow-skills 설치 가이드

Claude Code용 개발 워크플로우 플러그인 (Jira, Notion 연동)

---

## 📋 목차

1. [개요](#개요)
2. [설치 방법](#설치-방법)
3. [MCP 서버 설정](#mcp-서버-설정)
4. [프로젝트별 설정](#프로젝트별-설정)
5. [사용 가능한 명령어](#사용-가능한-명령어)
6. [플러그인 관리](#플러그인-관리)
7. [문제 해결](#문제-해결)

---

## 개요

### 플러그인이란?

Claude Code 플러그인은 슬래시 명령어(`/jira:list`, `/notion:search` 등)를 추가하여 개발 워크플로우를 자동화합니다.

### 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        GitHub                                │
│         oceansmart/dev-workflow-skills (저장소)              │
│                           │                                  │
│    ┌──────────────────────┴──────────────────────┐          │
│    │  marketplace.json, plugin.json, commands/   │          │
│    └──────────────────────┬──────────────────────┘          │
└───────────────────────────┼─────────────────────────────────┘
                            │
                            │ claude plugin install
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      개발자 PC                               │
│                                                              │
│  ┌─────────────────┐    ┌─────────────────────────────────┐ │
│  │  마켓플레이스    │    │        Claude Code              │ │
│  │  (소스 목록)     │───▶│  ~/.claude/plugins/             │ │
│  │                 │    │  (설치된 플러그인)               │ │
│  └─────────────────┘    └─────────────────────────────────┘ │
│                                                              │
│  ┌─────────────────┐    ┌─────────────────────────────────┐ │
│  │  .mcp.json      │    │  .claude/config/jira.md         │ │
│  │  (API 토큰)     │    │  (프로젝트별 설정)              │ │
│  │  ⚠️ PC마다 설정  │    │  ⚠️ 프로젝트마다 설정           │ │
│  └─────────────────┘    └─────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 설정 항목별 특성

| 항목 | 공유 가능? | 저장 위치 | 설정 시점 |
|------|-----------|----------|----------|
| **플러그인** | ✅ GitHub으로 공유 | `~/.claude/plugins/` | PC당 1회 |
| **MCP 토큰** | ❌ 보안상 공유 불가 | 프로젝트 `.mcp.json` | PC당 1회 |
| **프로젝트 설정** | ⚠️ 프로젝트마다 다름 | `.claude/config/jira.md` | 프로젝트당 1회 |

---

## 설치 방법

### 사전 요구사항

- Claude Code CLI 설치됨
- 터미널 접근 가능

### Step 1: 마켓플레이스 등록

마켓플레이스는 "플러그인을 어디서 다운로드할지" 알려주는 소스 목록입니다.

```bash
claude plugin marketplace add oceansmart/dev-workflow-skills
```

**확인:**
```bash
claude plugin marketplace list
```

출력 예시:
```
Configured marketplaces:

  ❯ claude-plugins-official
    Source: GitHub (anthropics/claude-plugins-official)

  ❯ dev-workflow-skills
    Source: GitHub (oceansmart/dev-workflow-skills)
```

### Step 2: 플러그인 설치

등록된 마켓플레이스에서 플러그인을 다운로드하여 설치합니다.

```bash
claude plugin install dev-workflow-skills
```

**확인:**
```bash
claude plugin list
```

출력 예시:
```
Installed plugins:

  ❯ dev-workflow-skills@dev-workflow-skills (user)
```

### Step 3: Claude Code 재시작

설치 후 Claude Code를 재시작해야 플러그인이 활성화됩니다.

```bash
# 현재 세션 종료
exit  # 또는 Ctrl+C

# 다시 시작
claude
```

### 설치 완료 확인

Claude Code 세션에서 명령어가 인식되는지 확인:

```
/jira:list
```

→ MCP 설정 안내가 나오면 정상입니다 (다음 단계로 진행)

---

## MCP 서버 설정

MCP(Model Context Protocol)는 Claude Code가 외부 서비스(Jira, Notion)와 통신하는 방식입니다.

### 자동 설정 (권장)

플러그인 명령어를 처음 실행하면 자동으로 설정을 안내합니다:

```
/jira:list
```

출력:
```
⛔ Jira MCP 연결이 설정되지 않았습니다.

/jira:* 명령어를 사용하려면 먼저 Jira MCP 서버 연결이 필요합니다.

📋 필요한 정보:
   • Jira Instance URL (예: https://company.atlassian.net)
   • Jira Email (Jira 로그인 이메일)
   • Jira API Token

MCP 설정을 진행할까요? (예/아니오)
```

"예"를 선택하고 안내에 따라 정보를 입력하면 `.mcp.json` 파일이 자동 생성됩니다.

### 수동 설정

프로젝트 루트에 `.mcp.json` 파일을 직접 생성할 수도 있습니다:

```json
{
  "mcpServers": {
    "jira": {
      "command": "uvx",
      "args": ["mcp-server-jira"],
      "env": {
        "JIRA_INSTANCE_URL": "https://your-company.atlassian.net",
        "JIRA_USER_EMAIL": "your-email@company.com",
        "JIRA_API_TOKEN": "your-api-token-here"
      }
    },
    "notion": {
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "OPENAPI_MCP_HEADERS": "{\"Authorization\": \"Bearer your-notion-token\", \"Notion-Version\": \"2022-06-28\"}"
      }
    }
  }
}
```

### API 토큰 발급 방법

#### Jira API Token

1. https://id.atlassian.com/manage-profile/security/api-tokens 접속
2. "Create API token" 클릭
3. Label 입력 (예: "Claude Code")
4. "Create" 클릭
5. 토큰 복사 (이 화면을 벗어나면 다시 볼 수 없음!)

#### Notion Integration Token

1. https://www.notion.so/my-integrations 접속
2. "New integration" 클릭
3. Name 입력 (예: "Claude Code")
4. Associated workspace 선택
5. Capabilities에서 "Read content", "Update content", "Insert content" 활성화
6. "Submit" 클릭
7. Internal Integration Token 복사

**⚠️ 중요: Notion 페이지에 Integration 연결 필요**
- 연결하려는 Notion 페이지 열기
- 우측 상단 "..." 메뉴 클릭
- "Connections" → 생성한 Integration 추가

### 보안 주의사항

```bash
# .mcp.json을 git에서 제외 (필수!)
echo ".mcp.json" >> .gitignore
```

`.mcp.json`에는 API 토큰이 포함되어 있으므로 **절대 GitHub에 push하면 안 됩니다**.

---

## 프로젝트별 설정

Jira 명령어는 프로젝트별 설정 파일이 필요합니다.

### 자동 설정 (권장)

MCP 설정 완료 후 Jira 명령어를 실행하면:

```
/jira:list
```

출력:
```
⛔ Jira 프로젝트 설정 파일이 없습니다.

📁 필요한 파일: .claude/config/jira.md

설정 파일을 생성할까요? (예/아니오)
```

"예"를 선택하고 다음 정보를 입력:

- **Project Key**: Jira 티켓 접두사 (예: `SMFD`, `PROJ`)
- **Project Name**: 프로젝트 전체 이름
- **Board ID**: Jira 보드 URL에서 확인 (`/board/3` → `3`)
- **Board Type**: `scrum` 또는 `kanban`

### 수동 설정

`.claude/config/jira.md` 파일을 직접 생성:

```markdown
# Jira Configuration - 프로젝트 이름

## Required Settings (필수 설정)

### Project Information
| 항목 | 값 | 설명 |
|------|-----|------|
| **Project Key** | `SMFD` | Jira 프로젝트 키 |
| **Project Name** | OceanSmart FMS | 프로젝트 전체 이름 |
| **Board ID** | `3` | Scrum/Kanban 보드 ID |
| **Board Type** | `scrum` | 보드 타입 (scrum/kanban) |

### Branch Naming Convention
| 타입 | 패턴 | 예시 |
|------|------|------|
| Feature | `feature/{issueKey}-{summary}` | `feature/SMFD-123-add-login` |
| Bugfix | `bugfix/{issueKey}-{summary}` | `bugfix/SMFD-124-fix-crash` |

### Default Assignee
- **Current User**: your-email@company.com
```

### Board ID 확인 방법

1. Jira에서 해당 프로젝트의 보드 열기
2. URL 확인: `https://company.atlassian.net/jira/software/projects/SMFD/boards/3`
3. `/boards/` 뒤의 숫자가 Board ID (위 예시에서는 `3`)

---

## 사용 가능한 명령어

### Jira 기본 명령어

| 명령어 | 설명 | 예시 |
|--------|------|------|
| `/jira:list` | 백로그 목록 조회 | `/jira:list` |
| `/jira:show` | 이슈 상세 정보 | `/jira:show SMFD-123` |
| `/jira:create` | 티켓 생성 | `/jira:create` |
| `/jira:start` | 작업 시작 (상태 변경 + 브랜치 생성) | `/jira:start SMFD-123` |
| `/jira:assign` | 담당자 지정 | `/jira:assign SMFD-123` |
| `/jira:implement` | 티켓 구현 | `/jira:implement SMFD-123` |
| `/jira:verify` | 완료 전 검증 | `/jira:verify SMFD-123` |
| `/jira:pr` | PR 생성 | `/jira:pr SMFD-123` |
| `/jira:merge` | PR 머지 및 브랜치 정리 | `/jira:merge SMFD-123` |

### Jira 구현 가이드라인

| 명령어 | 설명 |
|--------|------|
| `/jira:impl-backend-api` | Backend API 개발 구현 지침 |
| `/jira:impl-frontend` | Frontend 개발 구현 지침 |
| `/jira:impl-database` | Database 작업 구현 지침 |
| `/jira:impl-bugfix` | 버그 수정 구현 지침 |

### Notion 명령어

| 명령어 | 설명 | 예시 |
|--------|------|------|
| `/notion:search` | 페이지/데이터베이스 검색 | `/notion:search 회의록` |

---

## 플러그인 관리

### 설치된 플러그인 목록

```bash
claude plugin list
```

### 플러그인 업데이트

새 버전이 릴리즈되면:

```bash
# 마켓플레이스 캐시 업데이트
claude plugin marketplace update dev-workflow-skills

# 플러그인 업데이트
claude plugin update dev-workflow-skills

# Claude Code 재시작
```

### 플러그인 비활성화/활성화

```bash
# 비활성화
claude plugin disable dev-workflow-skills

# 활성화
claude plugin enable dev-workflow-skills
```

### 플러그인 제거

```bash
# 플러그인 제거
claude plugin uninstall dev-workflow-skills

# 마켓플레이스도 제거하려면
claude plugin marketplace remove dev-workflow-skills
```

---

## 문제 해결

### "Unknown skill: jira:list"

**원인**: 플러그인이 설치되지 않았거나 활성화되지 않음

**해결**:
```bash
# 플러그인 설치 확인
claude plugin list

# 설치 안 됐으면 설치
claude plugin marketplace add oceansmart/dev-workflow-skills
claude plugin install dev-workflow-skills

# Claude Code 재시작
```

### "Jira MCP 연결이 설정되지 않았습니다"

**원인**: `.mcp.json` 파일이 없거나 jira 설정이 없음

**해결**: 안내에 따라 MCP 설정 진행 (위 "MCP 서버 설정" 섹션 참조)

### "MCP 도구를 찾을 수 없습니다"

**원인**: `.mcp.json`은 있지만 Claude Code에 반영 안 됨

**해결**: Claude Code 재시작
```bash
# 세션 종료 후 다시 시작
exit
claude
```

### "Jira API 인증 실패"

**원인**: API Token이 만료되었거나 잘못됨

**해결**:
1. https://id.atlassian.com/manage-profile/security/api-tokens 에서 새 토큰 생성
2. `.mcp.json`의 `JIRA_API_TOKEN` 값 업데이트
3. Claude Code 재시작

### "프로젝트 설정 파일이 없습니다"

**원인**: `.claude/config/jira.md` 파일이 없음

**해결**: 안내에 따라 프로젝트 설정 생성 (위 "프로젝트별 설정" 섹션 참조)

### Notion "페이지를 찾을 수 없습니다"

**원인**: 해당 페이지에 Integration이 연결되지 않음

**해결**:
1. Notion에서 해당 페이지 열기
2. 우측 상단 "..." 메뉴 클릭
3. "Connections" 선택
4. 생성한 Integration 추가

---

## 요약: 새 PC 또는 새 프로젝트 체크리스트

### 새 PC에서 시작할 때

- [ ] 1. 마켓플레이스 등록: `claude plugin marketplace add oceansmart/dev-workflow-skills`
- [ ] 2. 플러그인 설치: `claude plugin install dev-workflow-skills`
- [ ] 3. Claude Code 재시작
- [ ] 4. MCP 토큰 설정 (첫 명령어 실행 시 자동 안내)
- [ ] 5. 프로젝트 설정 (첫 명령어 실행 시 자동 안내)

### 기존 PC에서 새 프로젝트 시작할 때

- [ ] 1. 프로젝트 설정만 필요 (플러그인/MCP는 이미 있음)
- [ ] 2. `/jira:list` 실행 → 프로젝트 설정 안내 따르기

---

*Last Updated: 2025-01-16*
*Plugin Version: 1.0.0*
